#ifndef _H_PAFRB_CONFIG
#define _H_PAFRB_CONFIG

#include <fstream>
#include <string>

using std::string;

struct config_s {
    float dend;
    float dstart;
};

void read_config(string file) {

}

#endif
